# R0 证据 01 — 未提交 Diff 冻结证据（Wave 0–10 返工纠偏）

冻结时间：2026-09-14（本地）
冻结原因：总审计否决「Wave 0–10 全部完成」；施工方立即停止提交，先把工作区两处未提交改动**原样冻存**为证据，禁止 reset/checkout 丢失。

## 0. 现场标识

| 项 | 值 |
|---|---|
| repo | `E:\OS开发\LCOS_GEN2` |
| branch | `frontend-reconstruction-v2` |
| HEAD | `685e02a5b7c0f698cdf356436f975cbd4b573964`（Wave 10 提交） |
| upstream main | `63188c2`（未 push、未合并） |
| Huabu upstream pin | `a3c411e1f655191344285141f08c4738fa6015f7` |
| 工作区状态（`git status --porcelain`） | 仅 2 个 ` M`：`TextNode.tsx`、`LcosProjectShell.tsx` |
| 未提交改动统计 | 2 files changed, 24 insertions(+), 3 deletions(-) |

文件指纹（SHA256 前 16 位，冻结时）：

```text
3BAA2214B6B96441  huabu/apps/web/src/components/Nodes/text/TextNode.tsx
4B0D08D8668C4855  huabu/apps/web/src/lcos/shell/LcosProjectShell.tsx
```

## 1. 冻结时的服务进程清单（R0-1 原始记录）

| port | pid | 进程 | 启动方式 | cwd | 数据落点 |
|---|---|---|---|---|---|
| 5173 | 24332 | node（Vite） | `pnpm dev`（huabu，本会话启动） | `huabu` | 无 |
| 3001 | 41468 | node（Huabu server） | 同上（`scripts/dev.mjs` 编排） | `huabu/apps/server` | `huabu/apps/server/data/storage/disk/workspaces.json` |
| 43121 | 32260 | node（Local Core） | `npm run dev:local-core`（仓库根，本会话启动） | `apps/local-core` | Core `.data`（`lcos-gen2-dev` 项目） |

说明：三者均来自同一 checkout；本次为**本会话新起的进程**，不是历史会话残留。审计要求的「fresh 进程复测」尚待 R0-2 执行（见 R0 Readback 第 6 问）。

## 2. 两处未提交 Diff 的完整原文

### 2.1 `LcosProjectShell.tsx`（候选修复 A：`ensureCanvas(recreate)` 透传）

```diff
@@ -82,7 +82,9 @@ export function LcosProjectShell({
                 surface={active}
                 canvasId={canvasBySurface[active]}
                 canvasNodeCount={mainNodeCount}
-                ensureCanvas={() => ensureCanvas(active)}
+                // 必须透传 recreate：Main 的「重新建立现场画布」按钮靠它强制重建，
+                // 丢掉这个参数会退回到返回旧（失效）canvasId，按钮看起来点了没反应。
+                ensureCanvas={(recreate?: boolean) => ensureCanvas(active, recreate)}
                 ensureError={ensureError}
               />
             ) : active === 'context' ? (
@@ -105,7 +107,7 @@ export function LcosProjectShell({
                 projectId={projectId}
                 surface={active}
                 canvasId={canvasBySurface[active]}
-                ensureCanvas={() => ensureCanvas(active)}
+                ensureCanvas={(recreate?: boolean) => ensureCanvas(active, recreate)}
                 ensureError={ensureError}
               />
             )}
```

现场成因（可复现）：`MainWorksiteProps.ensureCanvas: (recreate?: boolean) => Promise<string | undefined>`，而 Shell 传的是零参闭包 → `recreate` 恒为 `undefined` → `useLcosWorksite.ensureSurfaceCanvas(surface, force=false)` 命中 `if (!force && existing) return existing`，直接返回失效的旧 canvasId，按钮点了没反应。Context/Workflow 走 `ensureCanvas={ensureCanvas}` 直传，故只有 Main 坏。

### 2.2 `TextNode.tsx`（**审计已否决其作为最终设计**：逐 native node type 打 body 补丁）

```diff
@@ -9,6 +9,7 @@ import { resolveAccent } from '@huabu/shared';

 import { FloatingToolbar } from '@/components/Common/FloatingToolbar.tsx';
 import { useTextNodeSurface } from '@/hooks/useTextNodeSurface';
+import { useResolvedNodeBody } from '@/lcos-seam/nodeBodySlot';
 import useCanvasStore, { settleNodePreprocess } from '@/store/canvasStore.ts';
 import {
   FONT_FAMILY_CSS,
@@ -52,6 +53,13 @@ export const TextNode = memo(
     const content = data.content ?? '';
     const isContentMissing = data.contentMissing === true;

+    // LCOS host 的 body 替换点（无 host 扩展/无 binding → undefined，保持原生 text body）。
+    const BodyOverride = useResolvedNodeBody({
+      nodeId: id,
+      nodeType: 'text',
+      data: data as Readonly<Record<string, unknown>>,
+    });
+
     // ------------------------------------------------------------------
     // Style derivation
     // ------------------------------------------------------------------
@@ -225,7 +233,18 @@ export const TextNode = memo(
         className="transition-all duration-200"
         {...surface.nodeWrapperProps}
       >
-        {isContentMissing ? (
+        {/*
+          LCOS 薄接缝（Wave 10 补）：投影到 text 节点的 Core 实体（artifact/conversation）
+          由 host 在同一节点、同一几何/选中/undo 上换成物种 body；无 host 扩展或无 binding
+          时返回 undefined，TextNodeBody 原样渲染。
+        */}
+        {BodyOverride ? (
+          <BodyOverride
+            nodeId={id}
+            nodeType="text"
+            data={data as Readonly<Record<string, unknown>>}
+          />
+        ) : isContentMissing ? (
           <div
             className="flex items-center overflow-hidden"
             style={{
```

处置：**不作为最终设计保留**。它证明的是「投影到 text 节点的实体读不到 LCOS 物种 body」这一事实；正确落点是 NodeWrapper / 统一 NodePresentation Junction 单点解析（见 R0 Readback 第 3 问）。在 R2 统一 junction 落地前，本文件改动保持未提交状态，不接受为验收依据。

## 3. 触发本次冻结的真实现场记录

1. 本会话 `npm run dev:local-core` + `pnpm dev` 启动三服务（Core 43121 / server 3001 / Vite 5173，`HUABU_CONNECTION_TOKEN=dev-token`、`LOCAL_CORE_API_TOKEN=dev-token`）。
2. 首次进入 `/projects/lcos-gen2-dev/main` → 页面报「**该 Space 不存在，或已被删除。**」，同时出现「重新建立现场画布（回写 workspace）」按钮；控制台 404。
3. Core 侧 workspace 记录（HTTP 直查 `/lcos-core/projects/lcos-gen2-dev/workspaces`）：
   - `workspace-real-main` → `canvasId=canvas-lcos-main`（**Huabu server 查无此画布，404**）
   - `workspace-real-context` / `workspace-real-workflow` → 两个 UUID 画布
4. 点击「重新建立现场画布」：Main **无反应**（仍是 `canvas-lcos-main`）；Context/Workflow 成功（新 canvasId 写回 Core）。
5. 修复 2.1 后重试：Main 新建画布 `canvas-4bcac717-a67b-487c-855e-08c9357648aa`，Core 回写成功。
6. 新画布上 reconciler（`reconcile('project-open')`）投影出 **6 个 `react-flow__node-text`** 节点，`data.label` = `项目定位 / 项目定位 1 / …`，`data.origin.type='ai-operate'`，位置 `(0,0)`、`(40,40)`…；`.react-flow__node` 全为 `text` 类型。
7. LCOS 物种 body 计数 = **0**：`useResolvedNodeBody` 当时只挂在 `NoteNode`，text 节点无 junction。
8. 应用 2.2（TextNode 补 junction）后重测：物种 body 从 0 → **3**（另 3 个节点无 binding，按设计回退原生），无 console error。
9. 视觉结果（截图 `%TEMP%\trae\screenshots\live_species.png`）：3 张「材料」物种卡 + 原生 AI 文本节点**互相重度重叠**，相机「适合画面」把缩放推到 **348%**；标题字号被放大到难以阅读。
10. 用户裁定：该状态不可作为前端完成证据；节点落位（`index*40`）与单点 junction 都要返工，禁止为截图好看静默重排。

## 4. e2e「跑完即通过」的硬证据（审计第 5 问）

```text
Select-String -Path 'scripts\e2e\*.mjs' -Pattern 'process.exit|throw new Error'
→ 0 命中
```

即 `scripts/e2e/wave1..wave10` 全部脚本只有 `console.log(JSON)`，**没有任何失败退出路径**：关键 selector 缺失、关键 boolean 为 false、console error、实体为空、HTTP 非预期状态，统统不影响退出码（恒为 0）。因此「脚本跑到末尾」在证据链上等价于「什么都没断言」。Wave 10 handoff 里 `staleCanvasEntry.recoverButtonPresent=false` 这种真实失败也被同一份「PASS」输出掩盖。

## 5. 本次冻结不做的事（对齐停止指令）

- 不提交上述 2 处改动；不 push；不改 main；不重写历史；不删 archive。
- 不重排、不覆盖任何已有画布坐标。
- 不继续 T7 / motion / LOD / 新 UI。
- 不把 `BodyOverride` 逐 native node type 特例当作最终架构。
